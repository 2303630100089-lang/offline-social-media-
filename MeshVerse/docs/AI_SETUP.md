# Local AI Setup

MVP currently provides a local command router (`LocalAiAssistant`) for offline intents.

Planned model runtime integration:
- TFLite model registry
- llama.cpp JNI bindings
- quantized local model packs
