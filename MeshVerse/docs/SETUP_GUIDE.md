# Setup Guide

1. Install Android SDK + Java 17.
2. From the `MeshVerse` project root run:
   - `bash build-scripts/setup-dev-env.sh`
   - `bash build-scripts/build-apk.sh`
3. Install debug APK from `app/build/outputs/apk/debug/`.
