# Stability Report

- Local data flows (chat/feed/profile/settings) are stable in single-device mode.
- Multi-hop relay logic is implemented with deduplication and route TTL.
- Build reproducibility depends on Android plugin dependency access.
